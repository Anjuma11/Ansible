[defaults]
forks=2
#inventory=inventory.aws_ec2.yaml
inventory= inventory.ini
remote_user= ec2-user
private_key_file=/home/ec2_user/keys/ansible.pem


# All the variables that needs to be passed in command line can be given in ansible.cfg file
